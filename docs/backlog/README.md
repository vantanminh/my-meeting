# Backlog

Harness improvement backlog entities (`BL-###.md`) are managed with:

```bash
harness backlog add --title "…"
harness backlog close --id BL-001 --status implemented --outcome "…"
harness propose --commit
```

Do not hand-edit backlog entity files as an agent.
