<Window x:Class="MeetingAssistant.TrayFlyoutWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:localization="clr-namespace:MeetingAssistant.Services"
        Title="Meeting Assistant"
        Width="360" SizeToContent="Height"
        WindowStyle="None" AllowsTransparency="True" Background="Transparent"
        ResizeMode="NoResize" ShowInTaskbar="False" Topmost="True"
        UseLayoutRounding="True" SnapsToDevicePixels="True"
        Deactivated="Window_Deactivated"
        PreviewKeyDown="Window_PreviewKeyDown"
        localization:LocalizationService.IsEnabled="True">
    <Window.Resources>
        <Style x:Key="RecordingSection" TargetType="Border">
            <Setter Property="Visibility" Value="Collapsed" />
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsRecording}" Value="True"><Setter Property="Visibility" Value="Visible" /></DataTrigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="ProcessingSection" TargetType="Border">
            <Setter Property="Visibility" Value="Collapsed" />
            <Style.Triggers>
                <DataTrigger Binding="{Binding IsProcessing}" Value="True"><Setter Property="Visibility" Value="Visible" /></DataTrigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="ErrorSection" TargetType="Border">
            <Setter Property="Visibility" Value="Collapsed" />
            <Style.Triggers>
                <MultiDataTrigger>
                    <MultiDataTrigger.Conditions>
                        <Condition Binding="{Binding ProcessingHasError}" Value="True" />
                        <Condition Binding="{Binding IsProcessing}" Value="False" />
                    </MultiDataTrigger.Conditions>
                    <Setter Property="Visibility" Value="Visible" />
                </MultiDataTrigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="IdleSection" TargetType="Border">
            <Setter Property="Visibility" Value="Collapsed" />
            <Style.Triggers>
                <MultiDataTrigger>
                    <MultiDataTrigger.Conditions>
                        <Condition Binding="{Binding IsIdle}" Value="True" />
                        <Condition Binding="{Binding ProcessingHasError}" Value="False" />
                    </MultiDataTrigger.Conditions>
                    <Setter Property="Visibility" Value="Visible" />
                </MultiDataTrigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="StatusDot" TargetType="Ellipse">
            <Setter Property="Fill" Value="{DynamicResource MintBrush}" />
            <Style.Triggers>
                <DataTrigger Binding="{Binding ProcessingHasError}" Value="True"><Setter Property="Fill" Value="{DynamicResource CoralBrush}" /></DataTrigger>
                <DataTrigger Binding="{Binding IsProcessing}" Value="True"><Setter Property="Fill" Value="{DynamicResource GoldBrush}" /></DataTrigger>
                <DataTrigger Binding="{Binding IsRecording}" Value="True"><Setter Property="Fill" Value="{DynamicResource CoralBrush}" /></DataTrigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="DeterminateProgress" TargetType="ProgressBar" BasedOn="{StaticResource {x:Type ProgressBar}}">
            <Setter Property="IsIndeterminate" Value="True" />
            <Style.Triggers>
                <DataTrigger Binding="{Binding ShowProcessingPercent}" Value="True"><Setter Property="IsIndeterminate" Value="False" /></DataTrigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <Border Margin="12" CornerRadius="12" Background="{DynamicResource CardBrush}" BorderBrush="{DynamicResource LineBrush}" BorderThickness="1" Padding="18">
        <Border.Effect>
            <DropShadowEffect BlurRadius="18" ShadowDepth="3" Opacity="0.35" />
        </Border.Effect>
        <StackPanel>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <Border Width="34" Height="34" CornerRadius="10" Background="{DynamicResource MintBrush}" VerticalAlignment="Center">
                    <TextBlock Text="M" Foreground="{DynamicResource BrandInkBrush}" FontWeight="Bold" FontSize="17" HorizontalAlignment="Center" VerticalAlignment="Center" />
                </Border>
                <StackPanel Grid.Column="1" Margin="12,0,8,0" VerticalAlignment="Center">
                    <TextBlock Text="Meeting Assistant" FontSize="13" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" />
                    <StackPanel Orientation="Horizontal" Margin="0,3,0,0">
                        <Ellipse Style="{StaticResource StatusDot}" Width="7" Height="7" VerticalAlignment="Center" Margin="0,0,7,0" />
                        <TextBlock Text="{Binding BackgroundStatusTitle}" FontSize="11" Foreground="{DynamicResource MutedBrush}" TextTrimming="CharacterEllipsis" />
                    </StackPanel>
                </StackPanel>
                <Button Grid.Column="2" Style="{StaticResource IconButton}" Content="&#xE711;" FontFamily="{StaticResource IconFont}" FontSize="11"
                        ToolTip="Close" AutomationProperties.Name="Close" Click="Close_Click" VerticalAlignment="Top" />
            </Grid>

            <Border Style="{StaticResource RecordingSection}" Background="{DynamicResource CoralDimBrush}" CornerRadius="10" Padding="14" Margin="0,16,0,0">
                <StackPanel>
                    <TextBlock Text="{Binding RecordingIndicatorLabel}" Style="{StaticResource SectionLabel}" Foreground="{DynamicResource CoralBrush}" />
                    <TextBlock Text="{Binding RecordingTitle}" FontSize="13" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" Margin="0,8,0,0" />
                    <TextBlock Text="{Binding ElapsedLabel}" FontSize="30" FontWeight="SemiBold" Margin="0,2,0,0" />
                    <Button Style="{StaticResource DangerButton}" Command="{Binding StopRecordingCommand}" HorizontalAlignment="Stretch" Margin="0,10,0,0" MinHeight="38">
                        <StackPanel Orientation="Horizontal">
                            <Ellipse Width="8" Height="8" Fill="{DynamicResource CoralBrush}" VerticalAlignment="Center" Margin="0,0,8,0" />
                            <TextBlock Text="Stop &amp; process" Foreground="{DynamicResource CoralBrush}" />
                        </StackPanel>
                    </Button>
                </StackPanel>
            </Border>

            <Border Style="{StaticResource ProcessingSection}" Background="{DynamicResource CardAltBrush}" CornerRadius="10" Padding="14" Margin="0,16,0,0">
                <StackPanel>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="{Binding ProcessingMeetingTitle}" ToolTip="{Binding ProcessingMeetingTitle}" FontSize="13" FontWeight="SemiBold" TextTrimming="CharacterEllipsis" VerticalAlignment="Center" />
                        <TextBlock Grid.Column="1" Text="{Binding ProcessingElapsedLabel}" FontSize="13" FontWeight="SemiBold" Foreground="{DynamicResource GoldBrush}" Margin="12,0,0,0" VerticalAlignment="Center" ToolTip="Elapsed" />
                    </Grid>
                    <TextBlock Text="{Binding ProcessingStage}" FontSize="12" Foreground="{DynamicResource TextBrush}" TextWrapping="Wrap" Margin="0,8,0,0" />
                    <TextBlock Text="{Binding ProcessingDetail}" FontSize="11" Foreground="{DynamicResource MutedBrush}" TextWrapping="Wrap" Margin="0,4,0,0" />
                    <ProgressBar Style="{StaticResource DeterminateProgress}" Value="{Binding ProcessingPercent, Mode=OneWay}" Maximum="100" Height="6" Margin="0,12,0,0" />
                    <Button Style="{StaticResource GhostButton}" Command="{Binding CancelProcessingCommand}" Content="Cancel processing" FontSize="12" Foreground="{DynamicResource CoralBrush}" HorizontalAlignment="Right" Margin="0,8,-6,-4" />
                </StackPanel>
            </Border>

            <Border Style="{StaticResource ErrorSection}" Background="{DynamicResource CoralDimBrush}" CornerRadius="10" Padding="14" Margin="0,16,0,0">
                <StackPanel>
                    <TextBlock Text="{Binding ProcessingStage}" FontSize="12" FontWeight="SemiBold" Foreground="{DynamicResource CoralBrush}" TextWrapping="Wrap" />
                    <TextBlock Text="{Binding ProcessingMessage}" FontSize="11" Foreground="{DynamicResource CoralBrush}" TextWrapping="Wrap" Margin="0,4,0,0" />
                    <Button Style="{StaticResource SecondaryButton}" Command="{Binding RetryProcessingCommand}" Content="Retry processing" FontSize="12" HorizontalAlignment="Left" Margin="0,10,0,0" />
                </StackPanel>
            </Border>

            <Border Style="{StaticResource IdleSection}" Background="{DynamicResource CardAltBrush}" CornerRadius="10" Padding="14" Margin="0,16,0,0">
                <StackPanel>
                    <TextBlock Text="Nothing is running right now." FontSize="12" FontWeight="SemiBold" TextWrapping="Wrap" />
                    <TextBlock Text="Start a recording here or with Ctrl + Shift + R." FontSize="11" Foreground="{DynamicResource MutedBrush}" TextWrapping="Wrap" Margin="0,4,0,0" />
                    <Button Style="{StaticResource PrimaryButton}" Command="{Binding StartRecordingCommand}" HorizontalAlignment="Left" Margin="0,10,0,0" Padding="14,8">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock Text="&#xE720;" FontFamily="{StaticResource IconFont}" FontSize="13" VerticalAlignment="Center" Foreground="{DynamicResource BrandInkBrush}" />
                            <TextBlock Text="Start recording" VerticalAlignment="Center" Margin="8,0,0,0" Foreground="{DynamicResource BrandInkBrush}" />
                        </StackPanel>
                    </Button>
                </StackPanel>
            </Border>

            <Grid Margin="0,14,0,0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <Button Style="{StaticResource SecondaryButton}" Click="Open_Click" HorizontalAlignment="Stretch" MinHeight="38">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="&#xE8A7;" FontFamily="{StaticResource IconFont}" FontSize="12" VerticalAlignment="Center" Foreground="{DynamicResource MintBrush}" />
                        <TextBlock Text="Open Meeting Assistant" VerticalAlignment="Center" Margin="8,0,0,0" TextTrimming="CharacterEllipsis" />
                    </StackPanel>
                </Button>
                <Button Grid.Column="1" Style="{StaticResource GhostButton}" Click="Exit_Click" Margin="8,0,0,0" MinHeight="38" ToolTip="Exit Meeting Assistant">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="&#xE7E8;" FontFamily="{StaticResource IconFont}" FontSize="12" VerticalAlignment="Center" />
                        <TextBlock Text="Exit" VerticalAlignment="Center" Margin="8,0,0,0" />
                    </StackPanel>
                </Button>
            </Grid>
        </StackPanel>
    </Border>
</Window>
